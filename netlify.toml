# Netlify build settings, so a deploy needs no clicking in the dashboard.
# The SPA rewrite itself lives in public/_redirects, which Vite copies into
# dist/ on every build.

[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
